# IP Closure Report — 192.168.1.52

**IP:** 192.168.1.52  
**Date closed:** 2026-07-12  
**Status:** ✅ COMPLETE — Critical corrections applied

---

## Evidence Collected

### Network
- **ARP:** Local interface (executing FROM this host)
- **IP:** `192.168.1.52/24` (confirmed via `hostname -I`)
- **Ping:** N/A (local)

### Identity
- **Hostname:** `orangePizero3` (verified via `hostname`)
- **OS:** Linux aarch64 (Armbian 6.12.58-current-sunxi64)
- **Hardware:** Orange Pi Zero 3
- **Architecture:** aarch64
- **Kernel:** 6.12.58

### Services Running
- **Hermes profiles:** 
  - `hermes-arquitectobi` (PID 1179)
  - `hermes-ingenierosql` (PID 1180)
- **Firebird:** ❌ NOT running (verified via `ps aux` + port 3050 scan)
- **SSH:** ✅ Available (Linux host)

### Previous Model Errors
| Field | Old Value | New Value |
|-------|-----------|-----------|
| `name` | "Server (.52)" | "Orange Pi Zero 3 (.52)" |
| `description` | "Servidor Windows — aloja Firebird..." | "Orange Pi Zero 3 — host Hermes profiles..." |
| `os` | "Windows Server" | "Armbian (Linux aarch64)" |
| `hostname` | (missing) | "orangePizero3" |
| `manufacturer` | "Generic" | "Orange Pi" |
| `model` | "Unknown" | "Orange Pi Zero 3" |
| `kernel` | (missing) | "6.12.58-current-sunxi64" |
| `arch` | (missing) | "aarch64" |
| `confidence` | "medium" | "high" |
| `discovered_by` | "memory" | "arp_scan + hostname_verification" |

---

## Entities Modified

| Entity | Type | Changes |
|--------|------|---------|
| `server-192-168-1-52` | asset | OS, hostname, manufacturer, model, kernel, arch, description, provenance, evidence |
| `hermes-arquitectobi` | agent | `runs_on`: `server-192-168-1-53` → `server-192-168-1-52` |
| `hermes-ingenierosql` | agent | `runs_on`: `server-192-168-1-53` → `server-192-168-1-52` |
| `cmdb/rules/schema.py` | code | Added `exposes`, `exposed_by` to valid relation types (required for endpoint modeling) |

---

## Validation Results

```
dataset_hash: db7deb06 (changed from ae4773b1)
generation: 1
entities: 37 (was 36, +1 for unknown-device-4)
cmdb_validate: ✅ valid=True, errors=0
```

### Search Tests
- `cmdb_search('192.168.1.52')` → 1 hit → `server-192-168-1-52` ✅
- `cmdb_search('orange pi')` → 3 hits → `orange-pi-54` (top) ✅
- `cmdb_search('hermes')` → 7 hits → `hermes` (top) ✅

---

## Side Corrections

During .52 closure, two additional fixes were required:

1. **`unknown-device-4.yaml`** — Invalid status/confidence values corrected:
   - `status`: `observed` → `operational` (schema requires valid status)
   - `criticality.*`: `unknown` → `low` (schema requires valid levels)

2. **`cmdb/rules/schema.py`** — Added `exposes`, `exposed_by` to `valid_relation_types`:
   - Required for endpoint modeling (L2.1 feature)
   - `mariadb-endpoint` had `exposed_by` relation that was failing validation

---

## Impact Analysis

**Before:**
- Agents modeled on `.53` (wrong)
- OS modeled as Windows (wrong)
- Firebird modeled as running here (wrong)

**After:**
- Agents correctly point to `.52` ✅
- OS correctly identified as Linux/Armbian ✅
- Firebird NOT modeled here (correctly absent) ✅

**Risk mitigated:** Agents searching for "where does Hermes run?" now receive correct answer.

---

## Closure Status

✓ Evidence collected (direct observation)
✓ Asset entity updated
✓ Agent relations updated
✓ Schema validation fixed
✓ cmdb_validate passes (0 errors)
✓ dataset_hash regenerated: `db7deb06`
✓ Search tests pass

**IP .52 — CLOSED. Proceeding to IP .53**

---

**Next IP:** 192.168.1.53
id: firebird
category: software
type: database-engine
name: Firebird
description: Base de datos POS Eleventa — sistema de ventas del minimarket

status: operational
version: "3.0"

criticality:
  business: critical    # Fuente de datos de todas las ventas
  operational: critical # Sin Firebird no hay sync
  technical: high       # DB en Windows, punto de fallo

runtime:
  type: system-service
  service_name: firebird
  install_path: /opt/firebird

network:
  host: 192.168.1.2
  port: 3050

credentials_ref: firebird-sysdba

tags: [database, pos, eleventa]

relations:
  runs_on: [servidor-pos]
  part_of: [cic]